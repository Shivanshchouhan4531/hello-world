// write a program to calculate area of squre
#include <stdio.h>

int main() {
    // Write C code here
    int side,area;
    printf("enter side:");
    scanf("%d",&side);
    area = side *side;
    printf("area of square : %d",area);
    return 0;
}
